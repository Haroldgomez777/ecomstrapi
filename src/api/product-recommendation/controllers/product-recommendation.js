'use strict';

/**
 * product-recommendation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::product-recommendation.product-recommendation');
